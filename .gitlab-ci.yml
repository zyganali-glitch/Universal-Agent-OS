stages:
  - test
  - validate

test:
  stage: test
  image: python:3.11
  script:
    - pip install pytest pyyaml
    - pytest tests/ -v --tb=short

validate-governance:
  stage: validate
  image: node:20
  script:
    - node cli/index.js verify --package
